using System.Collections;
using System.Collections.Generic;
using UnityEngine;

// TODO: Make sure to change the name of this class when the refactor is complete
public class NewChallengeStateMachine : BaseStateMachine
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
