using System.Collections;
using System.Collections.Generic;
using UnityEngine;

// Defines legal interactions and assigns them a numeric value
public enum Interaction
{
    Forward = 0,
    Backward = 1,
    Left = 2,
    Right = 3,
    Interact = 4,
    Attack = 5
}
